{
	"name": "@maca134/rxjs-remote-client",
	"version": "1.0.3",
	"description": "Observerables over the network client",
	"main": "./dist",
	"scripts": {
		"update": "git add ./* & git commit -m update & git push & npm version patch",
		"postversion": "npm run build && npm publish",
		"build": "rimraf ./dist && npm run generate-barrels && tsc",
		"generate-barrels": "barrelsby --delete -d ./src"
	},
	"author": "maca134",
	"license": "UNLICENSED",
	"dependencies": {
		"rxjs": "^6.5.3"
	},
	"devDependencies": {
		"barrelsby": "^2.2.0",
		"rimraf": "^3.0.0",
		"ts-node": "^8.5.2",
		"typescript": "^3.7.2"
	}
}
